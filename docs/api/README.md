# API Docs

- `http-api-v1.md`: V1 HTTP API routes, payloads, and examples
- `mcp-tools-v1.md`: V1 MCP tool surface and transport usage
- `cli-v1.md`: V1 CLI commands and V2.1 config/MCP/TUI usability examples
- 推荐起手顺序：先看 `cli-v1.md` 的 `config check` / `mcp info` / `tui init` / `skills export`，再接入 HTTP 或 MCP
