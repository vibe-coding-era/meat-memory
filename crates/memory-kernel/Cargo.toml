[package]
name = "memory-kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
memory-assets = { path = "../memory-assets" }
memory-core = { path = "../memory-core" }
memory-domain = { path = "../memory-domain" }
memory-extract = { path = "../memory-extract" }
memory-index = { path = "../memory-index" }
memory-models = { path = "../memory-models" }
memory-observability = { path = "../memory-observability" }
memory-policy = { path = "../memory-policy" }
memory-sync = { path = "../memory-sync" }
memory-store-md = { path = "../memory-store-md" }
memory-store-pg = { path = "../memory-store-pg" }
serde.workspace = true
serde_json.workspace = true
rustls = { version = "0.23.37", default-features = false, features = ["std", "ring", "tls12"] }
time.workspace = true
tracing.workspace = true
ulid.workspace = true
webpki-roots = "1.0.6"

[dev-dependencies]
sqlx.workspace = true
tempfile = "3.20.0"
tokio.workspace = true
