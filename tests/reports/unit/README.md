# Unit Report Folder

Use this folder for unit-style report artifacts.

- `latest/workspace-unit.txt`: newest `cargo test --workspace --lib --bins --quiet` output
- `archive/`: timestamped historical copies
